# AI-RSS-Hub 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# LLM API 配置（必填）
OPENAI_API_KEY=your_api_key_here

# LLM API Base URL（可选，默认为 OpenAI）
# 可以替换为其他兼容 OpenAI 格式的服务商
# 例如 DeepSeek: https://api.deepseek.com
# 例如 Gemini (通过代理): https://your-gemini-proxy.com/v1
OPENAI_API_BASE=https://api.openai.com/v1

# LLM 模型名称（可选）
OPENAI_MODEL=gpt-3.5-turbo

# 数据库路径（可选，默认为当前目录下的 SQLite）
DATABASE_URL=sqlite:///./ai_rss_hub.db

# RSS 抓取间隔（小时，可选，默认 1 小时）
FETCH_INTERVAL_HOURS=1

# HTTP 请求超时时间（秒，可选）
REQUEST_TIMEOUT=30

# LLM API 超时时间（秒，可选）
LLM_TIMEOUT=30

# 总结最大长度（字符数，可选）
SUMMARY_MAX_LENGTH=100
