# ===========================================
# AI-RSS-Hub 环境变量配置
# ===========================================
# 说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填入实际的配置值
# 3. .env 文件不会被提交到 Git（已在 .gitignore）
# ===========================================

# -------------------------------------------
# LLM API 配置（必填）
# -------------------------------------------

# OpenAI API Key（必填）
# 获取地址：https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# API Base URL（可选，默认为 OpenAI）
# 如需使用其他兼容 OpenAI 接口的服务商，请修改此项
#
# OpenAI（默认）:
OPENAI_API_BASE=https://api.openai.com/v1
#
# DeepSeek（推荐，性价比高）:
# OPENAI_API_BASE=https://api.deepseek.com
#
# 其他兼容服务（如本地部署的 Ollama 等）:
# OPENAI_API_BASE=http://localhost:11434/v1

# LLM 模型名称（可选）
# OpenAI 模型：gpt-3.5-turbo, gpt-4, gpt-4-turbo 等
# DeepSeek 模型：deepseek-chat
OPENAI_MODEL=gpt-3.5-turbo

# -------------------------------------------
# 数据库配置（可选）
# -------------------------------------------

# 数据库连接 URL
# SQLite（默认，适合开发和小规模使用）:
DATABASE_URL=sqlite:///./ai_rss_hub.db
#
# PostgreSQL（生产环境推荐）:
# DATABASE_URL=postgresql://user:password@localhost/ai_rss_hub
#
# MySQL:
# DATABASE_URL=mysql://user:password@localhost/ai_rss_hub

# -------------------------------------------
# RSS 抓取配置（可选）
# -------------------------------------------

# RSS 抓取间隔（小时）
# 默认：1 小时执行一次
# 建议：根据 RSS 源更新频率调整，1-6 小时为宜
FETCH_INTERVAL_HOURS=1

# HTTP 请求超时时间（秒）
# 用于 RSS 源的网络请求超时
REQUEST_TIMEOUT=30

# -------------------------------------------
# AI 总结配置（可选）
# -------------------------------------------

# LLM API 超时时间（秒）
# AI 总结生成的最大等待时间
LLM_TIMEOUT=30

# 总结最大长度（字符数）
# AI 生成的摘要最多包含多少个字符
SUMMARY_MAX_LENGTH=100

# -------------------------------------------
# 应用配置（可选）
# -------------------------------------------

# 应用运行端口（如果使用 uvicorn 启动）
# PORT=8000

# 日志级别（DEBUG, INFO, WARNING, ERROR）
# LOG_LEVEL=INFO

# -------------------------------------------
# 快速配置示例
# -------------------------------------------

# 示例 1：使用 OpenAI GPT-3.5
# OPENAI_API_KEY=sk-proj-xxxxx
# OPENAI_API_BASE=https://api.openai.com/v1
# OPENAI_MODEL=gpt-3.5-turbo

# 示例 2：使用 DeepSeek（推荐）
# OPENAI_API_KEY=sk-xxxxx
# OPENAI_API_BASE=https://api.deepseek.com
# OPENAI_MODEL=deepseek-chat

# 示例 3：使用本地 Ollama
# OPENAI_API_KEY=ollama
# OPENAI_API_BASE=http://localhost:11434/v1
# OPENAI_MODEL=llama2
