# ================================
# 安全配置示例
# ================================
# 将以下配置添加到你的 .env 文件中

# ========== API Token ==========
# 用于管理操作认证的 API Token
# 生成方法: python scripts/generate_token.py
# ⚠️ 生产环境必须设置强随机 Token
API_TOKEN=your-super-secret-api-token-here-change-me

# ========== CORS 配置 ==========
# 允许的跨域源（逗号分隔）
# 示例: "http://localhost:3000,https://yourdomain.com"
# 留空则使用默认值（仅允许本地开发）
CORS_ORIGINS=http://localhost:3000,https://your-frontend.com

# ========== 速率限制 ==========
# 是否启用速率限制
RATE_LIMIT_ENABLED=true
# 时间窗口内的最大请求数
RATE_LIMIT_TIMES=60
# 时间窗口（秒）
RATE_LIMIT_SECONDS=60

# ========== 输入验证 ==========
# URL 最大长度
MAX_URL_LENGTH=2048
# 名称最大长度
MAX_NAME_LENGTH=200
# 分类最大长度
MAX_CATEGORY_LENGTH=50

# ========== 性能配置 ==========
# AI 摘要并发生成数量（默认 10）
# 根据你的 API 限制调整，过高可能导致限流
MAX_CONCURRENT_SUMMARIES=10
