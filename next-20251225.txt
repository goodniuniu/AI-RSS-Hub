================================================================================
                    AI-RSS-Hub 项目优化计划

项目经理: Claude Code
制定日期: 2025-12-25
版本: v1.0
状态: 待执行
================================================================================

📊 当前项目状态评估
================================================================================

✅ 已完成的里程碑:
  - 核心功能开发完成（RSS 抓取、AI 总结、API 服务）
  - 本地开发环境搭建完成
  - 基础文档齐全
  - 代码同步机制建立
  - 初步部署成功

⚠️ 识别的问题和痛点:
  1. 手动启动/停止，缺乏服务化管理
  2. 无自动重启机制，程序崩溃后需人工介入
  3. 日志分散，缺乏集中管理
  4. 缺乏运行监控和告警机制
  5. 部署流程需要多步手动操作
  6. 缺乏性能指标收集
  7. 没有备份和恢复机制
  8. 缺少 CI/CD 自动化流程

================================================================================
🎯 下一阶段目标（优先级排序）
================================================================================

【P0 - 关键优先级】必须完成，影响系统稳定性
  ✓ 服务化部署（systemd 服务）
  ✓ 日志管理和轮转
  ✓ 基础监控和健康检查

【P1 - 高优先级】重要但不紧急
  ✓ 一键部署脚本
  ✓ 数据库备份机制
  ✓ 性能监控和统计
  ✓ 错误告警机制

【P2 - 中优先级】提升开发体验
  ✓ Docker 容器化
  ✓ Git 版本管理规范
  ✓ 单元测试覆盖
  ✓ API 文档完善

【P3 - 低优先级】锦上添花
  ✓ 前端界面开发
  ✓ 用户认证系统
  ✓ 多数据库支持
  ✓ 微服务拆分

================================================================================
📋 详细优化方案
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 1: 服务化部署 (systemd)                                  [P0 关键] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  将应用作为系统服务运行，实现自动启动、自动重启、日志管理

【实施步骤】

1. 创建 systemd 服务文件: /etc/systemd/system/ai-rss-hub.service

   [Unit]
   Description=AI-RSS-Hub Service
   After=network.target

   [Service]
   Type=simple
   User=sam
   WorkingDirectory=/home/sam/Github/AI-RSS-Hub
   Environment="PATH=/home/sam/Github/AI-RSS-Hub/venv/bin"
   ExecStart=/home/sam/Github/AI-RSS-Hub/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
   Restart=always
   RestartSec=10
   StandardOutput=append:/var/log/ai-rss-hub/app.log
   StandardError=append:/var/log/ai-rss-hub/error.log

   [Install]
   WantedBy=multi-user.target

2. 创建日志目录和管理脚本

   sudo mkdir -p /var/log/ai-rss-hub
   sudo chown sam:sam /var/log/ai-rss-hub

3. 启用和管理服务

   sudo systemctl daemon-reload
   sudo systemctl enable ai-rss-hub
   sudo systemctl start ai-rss-hub
   sudo systemctl status ai-rss-hub

【预期效果】
  ✓ 系统启动时自动运行
  ✓ 程序崩溃后 10 秒内自动重启
  ✓ 日志集中管理
  ✓ 便于监控和维护

【工作量】1-2 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 2: 日志管理和轮转                                       [P0 关键] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  防止日志文件无限增长，实现日志分级、轮转、归档

【实施步骤】

1. 配置 Python logging

   在 app/config.py 中添加日志配置:

   import logging
   from logging.handlers import RotatingFileHandler

   def setup_logging():
       logger = logging.getLogger()
       logger.setLevel(logging.INFO)

       # 文件处理器 - 自动轮转
       file_handler = RotatingFileHandler(
           'logs/app.log',
           maxBytes=10*1024*1024,  # 10MB
           backupCount=5
       )
       file_handler.setFormatter(
           logging.Formatter(
               '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
           )
       )
       logger.addHandler(file_handler)

2. 配置 logrotate (系统级)

   创建 /etc/logrotate.d/ai-rss-hub:

   /var/log/ai-rss-hub/*.log {
       daily
       rotate 7
       compress
       delaycompress
       notifempty
       create 0644 sam sam
       sharedscripts
       postrotate
           systemctl reload ai-rss-hub > /dev/null 2>&1 || true
       endscript
   }

3. 添加日志查看命令

   # 实时查看日志
   journalctl -u ai-rss-hub -f

   # 查看最近的错误
   journalctl -u ai-rss-hub -p err -n 50

【预期效果】
  ✓ 日志文件不会占满磁盘
  ✓ 保留 7 天历史日志
  ✓ 自动压缩旧日志
  ✓ 便于问题排查

【工作量】2-3 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 3: 健康检查和监控系统                                   [P0 关键] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  实时监控服务状态，及时发现和响应异常

【实施步骤】

1. 增强健康检查 API

   在 app/api/routes.py 添加详细健康检查:

   @router.get("/api/health/detailed")
   def detailed_health_check(session: Session = Depends(get_session)):
       return {
           "status": "ok",
           "timestamp": datetime.now().isoformat(),
           "database": check_database_health(session),
           "llm": check_llm_health(),
           "feeds": get_feed_statistics(session),
           "articles": get_article_statistics(session),
           "system": {
               "uptime": get_uptime(),
               "memory": get_memory_usage(),
               "disk": get_disk_usage()
           }
       }

2. 添加状态统计端点

   @router.get("/api/stats")
   def get_statistics(session: Session = Depends(get_session)):
       return {
           "feeds": {
               "total": count_feeds(session),
               "active": count_active_feeds(session)
           },
           "articles": {
               "total": count_articles(session),
               "today": count_articles_today(session),
               "with_summary": count_articles_with_summary(session)
           },
           "performance": {
               "avg_fetch_time": get_avg_fetch_time(),
               "avg_summary_time": get_avg_summary_time(),
               "success_rate": get_success_rate()
           }
       }

3. 创建监控脚本

   创建 scripts/health_monitor.sh:

   #!/bin/bash
   # 每 5 分钟检查一次服务健康状态

   while true; do
       HEALTH=$(curl -s http://localhost:8000/api/health)
       if [ $? -ne 0 ] || [[ "$HEALTH" != *"ok"* ]]; then
           echo "$(date): Service unhealthy!" | tee -a /var/log/ai-rss-hub/monitor.log
           # 发送告警（邮件/钉钉/企业微信）
           # send_alert "AI-RSS-Hub 服务异常"
           # 尝试重启
           sudo systemctl restart ai-rss-hub
       fi
       sleep 300
   done

4. 添加 Prometheus 指标（可选）

   pip install prometheus-client

   from prometheus_client import Counter, Histogram, generate_latest

   fetch_counter = Counter('rss_fetch_total', 'Total RSS fetches')
   summary_histogram = Histogram('ai_summary_duration_seconds', 'AI summary duration')

   @router.get("/metrics")
   def metrics():
       return Response(generate_latest(), media_type="text/plain")

【预期效果】
  ✓ 实时了解服务运行状态
  ✓ 自动发现和恢复故障
  ✓ 收集性能指标
  ✓ 为优化提供数据支持

【工作量】4-6 小时
【风险】中

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 4: 一键部署脚本                                         [P1 高优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  简化部署流程，实现一条命令完成全部部署

【实施步骤】

创建 deploy.sh:

#!/bin/bash
# AI-RSS-Hub 一键部署脚本

set -e  # 遇到错误立即退出

echo "=========================================="
echo "   AI-RSS-Hub 一键部署脚本"
echo "=========================================="

# 1. 检查系统环境
echo "1. 检查系统环境..."
command -v python3 >/dev/null 2>&1 || { echo "需要 Python 3"; exit 1; }
command -v pip >/dev/null 2>&1 || { echo "需要 pip"; exit 1; }

# 2. 创建虚拟环境
echo "2. 创建虚拟环境..."
if [ ! -d "venv" ]; then
    python3 -m venv venv
fi

# 3. 激活虚拟环境并安装依赖
echo "3. 安装依赖..."
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

# 4. 配置环境变量
echo "4. 配置环境变量..."
if [ ! -f ".env" ]; then
    cp .env.example .env
    echo "请编辑 .env 文件配置 API Key"
    read -p "按回车键继续..."
fi

# 5. 初始化数据库（如果需要）
echo "5. 初始化数据库..."
# python -m app.database --init

# 6. 测试运行
echo "6. 测试服务..."
timeout 10 uvicorn app.main:app --host 0.0.0.0 --port 8000 || true

# 7. 安装 systemd 服务
echo "7. 安装系统服务..."
read -p "是否安装为系统服务? (y/n) " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    sudo cp deploy/ai-rss-hub.service /etc/systemd/system/
    sudo systemctl daemon-reload
    sudo systemctl enable ai-rss-hub
    sudo systemctl start ai-rss-hub
    echo "服务已安装并启动"
fi

echo "=========================================="
echo "   部署完成！"
echo "=========================================="
echo "访问: http://localhost:8000"
echo "文档: http://localhost:8000/docs"

【预期效果】
  ✓ 新环境部署时间从 30 分钟缩短到 5 分钟
  ✓ 减少人为错误
  ✓ 统一部署流程

【工作量】3-4 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 5: 数据库备份和恢复                                     [P1 高优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  防止数据丢失，支持快速恢复

【实施步骤】

1. 创建备份脚本 scripts/backup.sh:

   #!/bin/bash
   BACKUP_DIR="/home/sam/backups/ai-rss-hub"
   DB_FILE="/home/sam/Github/AI-RSS-Hub/ai_rss_hub.db"
   DATE=$(date +%Y%m%d_%H%M%S)

   mkdir -p $BACKUP_DIR

   # 备份数据库
   cp $DB_FILE "$BACKUP_DIR/ai_rss_hub_$DATE.db"

   # 压缩旧备份
   find $BACKUP_DIR -name "*.db" -mtime +1 -exec gzip {} \;

   # 删除 30 天前的备份
   find $BACKUP_DIR -name "*.gz" -mtime +30 -delete

   echo "备份完成: ai_rss_hub_$DATE.db"

2. 配置 cron 定时备份:

   # 每天凌晨 3 点备份
   0 3 * * * /home/sam/Github/AI-RSS-Hub/scripts/backup.sh

3. 创建恢复脚本 scripts/restore.sh:

   #!/bin/bash
   if [ -z "$1" ]; then
       echo "用法: ./restore.sh <备份文件>"
       exit 1
   fi

   # 停止服务
   sudo systemctl stop ai-rss-hub

   # 备份当前数据库
   cp ai_rss_hub.db ai_rss_hub.db.before_restore

   # 恢复
   if [[ $1 == *.gz ]]; then
       gunzip -c $1 > ai_rss_hub.db
   else
       cp $1 ai_rss_hub.db
   fi

   # 启动服务
   sudo systemctl start ai-rss-hub

   echo "恢复完成"

【预期效果】
  ✓ 自动每日备份
  ✓ 保留 30 天历史
  ✓ 快速恢复能力
  ✓ 数据安全保障

【工作量】2-3 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 6: Docker 容器化部署                                    [P2 中优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  实现跨平台部署，简化环境依赖

【实施步骤】

1. 创建 Dockerfile:

   FROM python:3.11-slim

   WORKDIR /app

   COPY requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt

   COPY app/ ./app/

   EXPOSE 8000

   CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

2. 创建 docker-compose.yml:

   version: '3.8'

   services:
     ai-rss-hub:
       build: .
       ports:
         - "8000:8000"
       volumes:
         - ./data:/app/data
         - ./logs:/app/logs
       env_file:
         - .env
       restart: unless-stopped
       healthcheck:
         test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
         interval: 30s
         timeout: 10s
         retries: 3

3. 使用 Docker 部署:

   # 构建镜像
   docker-compose build

   # 启动服务
   docker-compose up -d

   # 查看日志
   docker-compose logs -f

   # 停止服务
   docker-compose down

【预期效果】
  ✓ 环境隔离，避免依赖冲突
  ✓ 快速部署到任何支持 Docker 的环境
  ✓ 便于扩展和负载均衡
  ✓ 统一开发和生产环境

【工作量】4-6 小时
【风险】中

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 7: 性能监控仪表板                                       [P1 高优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  可视化运行状态，便于性能分析

【实施步骤】

1. 添加数据收集功能

   在数据库中创建 statistics 表:

   CREATE TABLE statistics (
       id INTEGER PRIMARY KEY,
       date DATE,
       feeds_fetched INTEGER,
       articles_added INTEGER,
       summaries_generated INTEGER,
       avg_fetch_time REAL,
       avg_summary_time REAL,
       success_rate REAL,
       created_at TIMESTAMP
   );

2. 创建统计 API

   @router.get("/api/dashboard/stats")
   def get_dashboard_stats(
       days: int = 7,
       session: Session = Depends(get_session)
   ):
       return {
           "overview": {
               "total_feeds": count_feeds(session),
               "total_articles": count_articles(session),
               "articles_today": count_articles_today(session),
               "success_rate": calculate_success_rate(session)
           },
           "trends": get_trends(session, days),
           "performance": {
               "avg_fetch_time": get_avg_metric(session, "fetch_time", days),
               "avg_summary_time": get_avg_metric(session, "summary_time", days)
           },
           "top_feeds": get_top_feeds(session, limit=5)
       }

3. 创建简单的 HTML 仪表板

   在 app/static/dashboard.html 中创建可视化页面

【预期效果】
  ✓ 直观了解系统运行情况
  ✓ 识别性能瓶颈
  ✓ 数据驱动优化决策

【工作量】6-8 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 8: 告警和通知系统                                       [P1 高优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  异常情况及时通知，减少故障时间

【实施步骤】

1. 添加通知配置

   在 .env 中添加:

   # 告警配置
   ALERT_EMAIL=your@email.com
   ALERT_WEBHOOK=https://hooks.slack.com/...  # Slack/钉钉
   ALERT_ENABLED=true

2. 创建告警服务 app/services/alerter.py:

   from enum import Enum
   import requests

   class AlertLevel(Enum):
       INFO = "info"
       WARNING = "warning"
       ERROR = "error"
       CRITICAL = "critical"

   def send_alert(level: AlertLevel, title: str, message: str):
       if not settings.alert_enabled:
           return

       # 发送到 Webhook（钉钉/Slack/企业微信）
       if settings.alert_webhook:
           send_webhook_alert(level, title, message)

       # 发送邮件
       if settings.alert_email:
           send_email_alert(level, title, message)

3. 在关键位置添加告警

   # RSS 抓取失败
   if failed_count > 3:
       send_alert(
           AlertLevel.WARNING,
           "RSS 抓取失败过多",
           f"连续 {failed_count} 次抓取失败"
       )

   # AI 总结成功率低
   if success_rate < 0.5:
       send_alert(
           AlertLevel.ERROR,
           "AI 总结成功率过低",
           f"当前成功率: {success_rate:.2%}"
       )

   # 磁盘空间不足
   if disk_usage > 0.9:
       send_alert(
           AlertLevel.CRITICAL,
           "磁盘空间不足",
           f"使用率: {disk_usage:.2%}"
       )

【预期效果】
  ✓ 问题及时发现
  ✓ 减少人工巡检
  ✓ 提高响应速度

【工作量】4-5 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 9: Git 版本管理规范                                     [P2 中优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  规范代码管理，支持团队协作

【实施步骤】

1. 初始化 Git 仓库

   cd /home/sam/Github/get_news_api/AI-RSS-Hub
   git init
   git add .
   git commit -m "chore: initial commit"

2. 配置分支策略

   - main/master: 生产环境代码
   - develop: 开发环境代码
   - feature/*: 新功能分支
   - hotfix/*: 紧急修复分支

3. 创建 .gitignore（已有，确认完整）

   venv/
   __pycache__/
   *.pyc
   .env
   *.db
   *.log
   .DS_Store

4. 添加提交规范（Conventional Commits）

   feat: 新功能
   fix: 修复 bug
   docs: 文档更新
   style: 代码格式
   refactor: 重构
   test: 测试
   chore: 构建/工具

5. 配置 pre-commit hooks

   创建 .git/hooks/pre-commit:

   #!/bin/bash
   # 运行代码检查
   black app/ --check
   flake8 app/
   # 运行测试
   pytest tests/

【预期效果】
  ✓ 代码版本可追溯
  ✓ 支持多人协作
  ✓ 代码质量提升

【工作量】2-3 小时
【风险】低

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ 方案 10: 自动化测试                                          [P2 中优] │
└─────────────────────────────────────────────────────────────────────────┘

【目标】
  提高代码质量，防止回归

【实施步骤】

1. 创建测试目录结构

   tests/
   ├── __init__.py
   ├── conftest.py          # pytest 配置
   ├── test_api.py          # API 测试
   ├── test_rss_fetcher.py  # RSS 抓取测试
   ├── test_summarizer.py   # AI 总结测试
   └── test_models.py       # 模型测试

2. 编写测试用例

   # tests/test_api.py
   from fastapi.testclient import TestClient
   from app.main import app

   client = TestClient(app)

   def test_health_check():
       response = client.get("/api/health")
       assert response.status_code == 200
       assert response.json()["status"] == "ok"

   def test_list_feeds():
       response = client.get("/api/feeds")
       assert response.status_code == 200
       assert isinstance(response.json(), list)

3. 运行测试

   # 运行所有测试
   pytest

   # 生成覆盖率报告
   pytest --cov=app --cov-report=html

4. 集成到 CI/CD

   创建 .github/workflows/test.yml:

   name: Tests
   on: [push, pull_request]
   jobs:
     test:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v2
         - uses: actions/setup-python@v2
         - run: pip install -r requirements.txt
         - run: pytest

【预期效果】
  ✓ 自动发现回归问题
  ✓ 提高代码信心
  ✓ 减少手动测试

【工作量】8-10 小时
【风险】中

================================================================================
📅 实施计划和时间表
================================================================================

阶段一: 稳定运行（1-2 周）
  Week 1:
    Day 1-2: 方案 1 - systemd 服务化
    Day 3-4: 方案 2 - 日志管理
    Day 5-7: 方案 3 - 健康检查和监控

  Week 2:
    Day 1-2: 方案 5 - 数据库备份
    Day 3-4: 方案 4 - 一键部署脚本
    Day 5-7: 测试和文档完善

阶段二: 监控告警（1 周）
  Week 3:
    Day 1-3: 方案 7 - 性能监控仪表板
    Day 4-6: 方案 8 - 告警系统
    Day 7: 集成测试

阶段三: 工程化（2 周）
  Week 4-5:
    Day 1-4: 方案 9 - Git 版本管理
    Day 5-8: 方案 10 - 自动化测试
    Day 9-10: 方案 6 - Docker 容器化
    Day 11-14: 文档和培训

================================================================================
💰 资源需求
================================================================================

人力资源:
  - 后端开发: 1 人（全职 2-3 周）
  - 运维工程师: 0.5 人（兼职支持）
  - 测试: 0.5 人（兼职支持）

硬件资源:
  - 服务器: 当前配置足够（2C4G）
  - 存储: 建议增加备份存储空间（50GB）
  - 监控: 无额外需求

软件资源:
  - 所有工具均为开源免费
  - 告警可选择免费的钉钉/飞书 webhook

预算:
  - 开发成本: 约 15 人天（如外包约 15,000-20,000 元）
  - 服务器成本: 无新增
  - 第三方服务: 0 元（使用免费方案）

总预算: 15,000-20,000 元（如自己开发则为 0）

================================================================================
🎯 关键成功指标（KPI）
================================================================================

可用性指标:
  ✓ 服务可用率: > 99.5%
  ✓ 平均故障恢复时间(MTTR): < 5 分钟
  ✓ 自动重启成功率: > 95%

性能指标:
  ✓ API 响应时间: < 200ms (p95)
  ✓ RSS 抓取成功率: > 95%
  ✓ AI 总结成功率: > 90%

运维指标:
  ✓ 部署时间: < 5 分钟
  ✓ 日志查询响应: < 3 秒
  ✓ 告警及时性: < 1 分钟

质量指标:
  ✓ 代码测试覆盖率: > 70%
  ✓ 代码规范检查通过率: 100%
  ✓ 安全漏洞: 0 个

================================================================================
⚠️ 风险和应对措施
================================================================================

风险 1: 服务迁移期间停机
  影响: 高 | 概率: 中
  应对: 在非高峰时段迁移，准备回滚方案

风险 2: Docker 化后性能下降
  影响: 中 | 概率: 低
  应对: 充分测试，对比性能指标

风险 3: 监控系统消耗过多资源
  影响: 中 | 概率: 低
  应对: 采样监控，控制采集频率

风险 4: 备份恢复失败
  影响: 高 | 概率: 低
  应对: 定期测试恢复流程

风险 5: 团队学习成本
  影响: 低 | 概率: 中
  应对: 编写详细文档，提供培训

================================================================================
📚 参考文档
================================================================================

系统管理:
  - systemd 服务管理: https://www.freedesktop.org/software/systemd/man/
  - logrotate 配置: https://linux.die.net/man/8/logrotate

容器化:
  - Docker 官方文档: https://docs.docker.com/
  - Docker Compose: https://docs.docker.com/compose/

监控:
  - Prometheus: https://prometheus.io/docs/
  - FastAPI 健康检查: https://fastapi.tiangolo.com/

测试:
  - pytest 文档: https://docs.pytest.org/
  - FastAPI 测试: https://fastapi.tiangolo.com/tutorial/testing/

最佳实践:
  - 12-Factor App: https://12factor.net/
  - Python 项目结构: https://docs.python-guide.org/writing/structure/

================================================================================
✅ 行动项和责任人
================================================================================

立即开始（本周）:
  [ ] 创建 systemd 服务文件               责任人: 开发者
  [ ] 配置日志轮转                         责任人: 开发者
  [ ] 实现健康检查 API                     责任人: 开发者
  [ ] 编写备份脚本                         责任人: 开发者

短期目标（1-2 周）:
  [ ] 完成一键部署脚本                     责任人: 开发者
  [ ] 实现监控仪表板                       责任人: 开发者
  [ ] 配置告警系统                         责任人: 开发者

中期目标（1 个月）:
  [ ] Docker 容器化                        责任人: 开发者
  [ ] 自动化测试覆盖                       责任人: 测试
  [ ] Git 工作流规范                       责任人: 团队

长期目标（3 个月）:
  [ ] CI/CD 流水线                         责任人: 运维
  [ ] 性能优化                             责任人: 开发者
  [ ] 功能扩展                             责任人: 产品/开发

================================================================================
📞 联系方式
================================================================================

项目经理: Claude Code
技术支持: 查看项目 README
问题反馈: https://github.com/yourusername/ai-rss-hub/issues

================================================================================
📝 更新日志
================================================================================

2025-12-25 v1.0
  - 初始版本
  - 制定 10 大优化方案
  - 确定实施计划和时间表
  - 明确资源需求和成功指标

下次更新: 根据实施进度更新

================================================================================
附录：快速参考命令
================================================================================

服务管理:
  sudo systemctl start ai-rss-hub      # 启动服务
  sudo systemctl stop ai-rss-hub       # 停止服务
  sudo systemctl restart ai-rss-hub    # 重启服务
  sudo systemctl status ai-rss-hub     # 查看状态
  journalctl -u ai-rss-hub -f          # 查看日志

备份恢复:
  ./scripts/backup.sh                   # 手动备份
  ./scripts/restore.sh backup.db        # 恢复备份

监控:
  curl http://localhost:8000/api/health        # 健康检查
  curl http://localhost:8000/api/stats         # 统计信息
  curl http://localhost:8000/api/dashboard     # 仪表板数据

部署:
  ./deploy.sh                           # 一键部署
  ./sync_to_deploy.sh                   # 代码同步
  docker-compose up -d                  # Docker 部署

测试:
  pytest                                # 运行测试
  pytest --cov=app                      # 测试覆盖率

================================================================================
结语
================================================================================

本优化计划基于当前项目状态制定，旨在提升系统的稳定性、可维护性和可观测性。
建议按照优先级逐步实施，每个阶段完成后进行评估和调整。

重点关注:
1. 稳定运行是首要目标（P0 方案）
2. 监控告警不可缺少（P1 方案）
3. 工程化提升长期效率（P2-P3 方案）

预期在 4-6 周内完成核心优化，使项目进入稳定运行状态。

祝项目成功！

================================================================================
